/**
 * Public entry for the project Issues page.
 *
 * Implementation lives in `./project-issues/` — start at
 * `project-issues/ProjectIssuesClient.tsx` for the page map, then open the
 * focused module for the piece you need (filters, table row, hooks, etc.).
 */

export { ProjectIssuesClient } from "./project-issues/ProjectIssuesClient";
