/** Preset swatches for new takeoff lines (sidebar + form). */
export const TAKEOFF_COLOR_PRESETS = [
  "#2563eb",
  "#16a34a",
  "#ea580c",
  "#dc2626",
  "#9333ea",
] as const;

/** Default takeoff item / draft preview color (matches first preset). */
export const DEFAULT_TAKEOFF_COLOR = TAKEOFF_COLOR_PRESETS[0];
